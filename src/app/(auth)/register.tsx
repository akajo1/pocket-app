import { RegisterTemplate } from "@/src/entities/auth/templates";
import React from "react";

type Props = {};

const register = (props: Props) => {
  return <RegisterTemplate />;
};

export default register;
